import styled from 'styled-components';

export const Globe = styled.div`
	place-self: center;
	color: var(--accent);
	font-weight: 300;
	font-size: 128px;
`;

export default styled.div`
	display: grid;
`;
